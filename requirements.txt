# Carbon evals — HF backend (Carbon, GENERator, any HF causal LM).
# Tested versions; loose pins on torch so users can match their CUDA build.
torch>=2.5,<2.7
transformers==4.57.1
tokenizers==0.22.1
datasets==4.8.4
accelerate>=1.7.0
huggingface_hub==0.36.2
scikit-learn==1.6.1
numpy==1.26.4
pandas==2.2.3
tqdm==4.67.1
